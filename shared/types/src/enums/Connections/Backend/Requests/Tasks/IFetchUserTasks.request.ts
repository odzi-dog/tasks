// @interface FetchUserTasksRequest
// - Contains all data, that needs to be
// sent to backend to return all users tasks of a particular user
export interface IFetchUserTasksRequest {
  id: string,
};